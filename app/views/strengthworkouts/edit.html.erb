
<div id="section_header" style="min-height: 0;">
    <div class="container">
        <div class="row">
                <div class="section_header">
                    <h5 class="blue">TRAINER TOOLS</h5>
                    <h3>Customize the <%= @workout.template %> for <%= User.find(@workout.user_ids[0]).email %></h3>
                </div>  
				<%= render partial: 'formatting' %>
				<%= render partial: 'workouts/strengthworkout' %>